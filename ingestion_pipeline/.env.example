# Example environment variables
TEMP_DIR=./tmp

# ChromaDB settings
# For local dev: localhost | For Docker: set CHROMA_HOST=chroma in docker-compose
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_COLLECTION=getmee_docs_dev

# Ingestion server
INGESTION_PORT=8001
